// Layers
// 0 (default to invisible) half/quarter view of current coords
// 1 Current, best viewing depth
// 2 Loading area for double/quadruple view of current coords
// 3 Duplicate of layer 0 as a backdrop
import { Container, Sprite, loader } from 'pixi.js'

export default class ImageTile {
  constructor() {}
}
